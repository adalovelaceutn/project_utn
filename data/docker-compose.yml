services:
  mongodb:
    image: mongo:7
    container_name: kolb_mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: kolb_user
      MONGO_INITDB_ROOT_PASSWORD: kolb_pass
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
